openapi: 3.0.3
info:
  title: La Llista de la Compra API
  version: 1.0.0
servers:
  - url: http://127.0.0.1:8080
paths: {}
components:
  schemas: {}
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
